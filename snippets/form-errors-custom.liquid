{% comment %}
  We want control over our error text, so we'll loop through them.

  Alternatively, you can use the default layout to generate a
  <ul> wrapped with <div class="errors">
    - {{ form.errors | default_errors }}
{% endcomment %}
{% if form.errors %}
  <div class="note form-error">
    <p>Có lỗi xảy ra, vui lòng nhập và gửi lại:</p>

    <ul class="disc">
      {% for field in form.errors %}

        {% if field == 'form' %}
          <li>
            {{ form.errors.messages[field] }}
          </li>
        {% else %}
          <li>
            <strong>{{ field | replace: 'body', 'Message' }}</strong> có lỗi {{ form.errors.messages[field] }}
          </li>
        {% endif %}

      {% endfor %}
     </ul>
  </div>
{% endif %}
